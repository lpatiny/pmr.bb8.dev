# Local development — copy this file to .env for running outside Docker.
# For Docker deployment, use .env.docker.example instead.

# Port the backend API listens on (the frontend dev server runs on 5173)
PORT=3000
