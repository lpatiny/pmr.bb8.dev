{
  "name": "oostende-bruges-backend",
  "version": "0.0.0",
  "description": "Fastify proxy that returns only PMR/bike-accessible trains between Oostende and Bruges",
  "type": "module",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "dev": "node --watch --use-system-ca --experimental-strip-types src/server.ts",
    "eslint": "eslint src",
    "eslint-fix": "eslint src --fix",
    "prettier": "prettier --check src",
    "prettier-write": "prettier --write src",
    "start": "node --use-system-ca --experimental-strip-types src/server.ts",
    "test": "npm run test-only && npm run check-types",
    "test-only": "vitest run --coverage",
    "test-watch": "vitest"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "@fastify/swagger": "^9.5.2",
    "@fastify/swagger-ui": "^5.2.3",
    "@fastify/type-provider-typebox": "^6.1.0",
    "@sinclair/typebox": "^0.34.49",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^4.1.5",
    "@zakodium/tsconfig": "^1.0.5",
    "vitest": "^4.1.5"
  },
  "volta": {
    "extends": "../package.json"
  }
}
