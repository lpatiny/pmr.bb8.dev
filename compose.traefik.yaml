# Traefik deployment — requires an external Docker network named "traefik".
# cp .env.example .env   # uncomment: COMPOSE_FILE=compose.traefik.yaml
# docker compose up -d
services:
  pmr.bb8.dev:
    image: ghcr.io/lpatiny/pmr.bb8.dev:latest
    build: .
    env_file: .env
    networks:
      - default
      - traefik
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.pmr-bb8-dev.rule=Host(`pmr.bb8.dev`)'
      - 'traefik.http.routers.pmr-bb8-dev.entrypoints=websecure'
      - 'traefik.http.routers.pmr-bb8-dev.tls.certresolver=letsencrypt'
      - 'traefik.http.services.pmr-bb8-dev.loadbalancer.server.port=${PORT:-3000}'
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 1
    pids_limit: 256
    ulimits:
      core: 0
    logging:
      options:
        max-size: '10m'
        max-file: '3'
    restart: unless-stopped

networks:
  traefik:
    external: true
